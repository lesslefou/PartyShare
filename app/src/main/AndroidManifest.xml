<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lisa.duterte.partyshare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LocationChoice"></activity>
        <activity android:name=".DrinkChoice" />
        <activity android:name=".FoodChoice" />
        <activity android:name=".ContactChoice" />
        <activity android:name=".Create_Activity_Adapter" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".Create_Activity" />
        <activity android:name=".NameChoiceActivity" />
        <activity android:name=".Add_Contact" />
        <activity android:name=".Setting" />
        <activity android:name=".Welcome" />
        <activity android:name=".Sign_Up" />
        <activity android:name=".Log_In" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>